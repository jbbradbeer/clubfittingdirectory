# Bug-Fix Plan — Code Audit 2026-07-01

Findings from a full code audit (Request a Fitting feature + general site health).

## Priority 1 — Blockers (Request a Fitting doesn't work until these are done)

- [ ] **Owner action: set missing environment variables** (in Vercel → Settings → Environment Variables, and in `web/.env.local`):
  - `ADMIN_PASSWORD` — without it the admin leads dashboard can't be logged into
  - `RESEND_API_KEY` — without it lead notification emails silently never send
  - `SUPABASE_SERVICE_ROLE_KEY` — currently a placeholder ("PASTE-NEW-sb_secret-KEY-HERE"); admin dashboard queries fail without the real key
- [ ] **Owner action: run the fitting-requests database migration** (`web/supabase/003_fitting_requests.sql`) in the Supabase SQL Editor, if not already run

## Priority 2 — Real bugs to fix in code

- [ ] **Silent 1,000-shop cap on state/category/city pages** — `web/lib/supabase/queries/shops.ts` lines 408, 443, 533 use `limit(1000)`; switch to `fetchAllRows()` (this exact class of bug made ~267 shops vanish from Google in June)
- [ ] **Directory pagination can duplicate/skip shops** — `web/lib/supabase/queries/listings.ts:74` needs a stable `order("id")` tiebreaker before `.range()`
- [ ] **HTML not escaped in lead notification emails** — `web/lib/email.ts:36`; escape user-typed values (name, notes, etc.) before putting them in the email HTML
- [ ] **Admin status updates fail silently** — `web/app/admin/leads/actions.ts:18-19` ignores database errors; check and surface them
- [ ] **No rate limiting on the fitting form** — `web/app/api/request-fitting/route.ts` has only a honeypot; add simple per-IP rate limiting
- [ ] **Email validation rejects valid addresses** — regex in `route.ts:50` rejects `name+tag@…`; loosen it
- [ ] **Duplicate migration number** — two files named `003_*.sql`; rename `003_fitting_requests.sql` → `004_fitting_requests.sql`

## Priority 3 — Nice-to-haves (later)

- [ ] Remove hardcoded fallback email in `web/lib/email.ts:20` (env-only)
- [ ] Tell users when notes exceed 1,000 chars instead of cutting silently
- [ ] Basic phone-number format check on the form
- [ ] Tidy repo root: move/delete stray Python scripts, CSVs, and the GSC zip
- [ ] (Optional, later) CAPTCHA, audit logging for admin actions

## What's healthy (verified — no action needed)

TypeScript builds clean; admin login uses secure password checking; database
row-level security is correct; sitemap, metadata, and route structure all check out.

## Review

_(to fill in after fixes)_
